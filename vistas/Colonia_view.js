
class ColoniaView {
    constructor(canvasContext) {
        this.canvasContext = canvasContext;
    }

    renderizar(colonia) {
        // Código para dibujar la colonia en el canvas utilizando this.canvasContext
        // Por ejemplo: Dibujar un círculo representando la colonia en su ubicación
    }
}

module.exports = ColoniaView;
