
class ColoniaController {
    constructor(colonia, coloniaView) {
        this.colonia = colonia;
        this.coloniaView = coloniaView;
    }

    manejarEntradaUsuario(entrada) {
        // Código para manejar la entrada del usuario
        // Por ejemplo: Si el usuario hace clic en una ubicación, mover la colonia allí
    }
}

module.exports = ColoniaController;
